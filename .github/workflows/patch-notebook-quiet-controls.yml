name: Patch notebook quiet controls

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-notebook-quiet-controls.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/patch_notebook_quiet_controls.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx
          git diff --cached --quiet && exit 0
          git commit -m "Hide notebook utility labels and restore created time"
          git push
